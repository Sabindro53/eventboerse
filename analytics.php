<?php
/**
 * DEAKTIVIERT (2026-06-20, DSGVO-Review)
 *
 * Diese Datei hat zuvor bei JEDEM Direktaufruf die Client-IP-Adresse
 * (ein personenbezogenes Datum i.S.d. DSGVO) ohne Rechtsgrundlage und ohne
 * Erwähnung in der Datenschutzerklärung in eine ungeschützte Flat-File
 * ("analytics.log") geschrieben. Sie war an keiner Stelle eingebunden
 * (toter Code) und widersprach der ausdrücklichen Plattform-Aussage
 * "ausschließlich technisch notwendige Cookies, keine Analyse-Cookies".
 *
 * Inhalt vollständig entfernt, damit der nächste Deploy die Live-Datei mit
 * dieser inerten Version überschreibt: kein Tracking, kein Logging, keine
 * Verarbeitung personenbezogener Daten mehr. Kann nach Bestätigung, dass die
 * Live-Datei überschrieben wurde, komplett gelöscht werden.
 */

// Bewusst leer: keine Verarbeitung personenbezogener Daten.
return;
